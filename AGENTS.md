# meo-gpt-connector

This repository is retired and no longer deployed. Do not restore its CI,
deployment stacks, public endpoints, or GPT OAuth bridge unless the owner
explicitly reverses the retirement decision.

Use `../meo-mcp` for agent integrations. The connector source remains only as a
historical reference for its ChatGPT Actions adapter and semantic tool shapes.
